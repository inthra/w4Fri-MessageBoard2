import { avatarGenerator } from 'message-board/helpers/avatar-generator';
import { module, test } from 'qunit';

module('Unit | Helper | avatar generator');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = avatarGenerator([42]);
  assert.ok(result);
});
